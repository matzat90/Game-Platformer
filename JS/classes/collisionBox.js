class CollisionBox {
    constructor
    ({
        positionX: x,
        positionY: y
    })
    {
        this.x = x;
        this.y = y;
        this.width = 50;
        this.height = 50;
    }

    update(){
    //ctx.fillStyle = "rgba(0,0,255,0.5)"
    //ctx.fillRect(this.x,this.y,this.width, this.height);
    }
}